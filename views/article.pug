extend layout

block content
  h1= article.title
  h3 Company ID  is #{article.author}
  h4 The average Salary provided by #{article.title}  is Rs.
    b #{article.salary}
  h4 They are basically looking for following skills : 
    b #{article.skill}
  h5 
    i= article.body

  a.btn.btn-default(href='/articles/edit/'+article._id) Edit
  a.btn.btn-danger.delete-article(href='#', data-id=article._id) Delete
